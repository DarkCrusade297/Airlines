@model Airlines.Models.BookModel
@using Airlines.Models

@{
    ViewBag.Title = "ChooseSeats";
}

@{ List<Customer> list = new List<Customer>();
    Customer customers = new Customer() { Surname = "", Name = "", Patronymic = "fgfg", Sex = ' ', BirthDate = "10.10.2010".AsDateTime(), Citizenship = " " };
    list.Add(customers);
    list.Add(customers);}
<h2>Информация о пассажирах</h2>
<h3>Внимательно введите данные пассажиров</h3>
@using (Html.BeginForm("Confirmation", "Book", FormMethod.Post))
{
    for (int i=0; i<Model.Person;i++)
        <h2>1  пассажир</h2>
        @Html.EditorFor(m =>customers)
        @Html.EditorForModel()
    <button type="submit" class="btn btn-primary desight">Подтвердить</button>
}


<div class='Bresult'>
</div>
<div class='Eresult'>
</div>
<div class="container planeModel">
    <div class="rows">
        <div class="buisness"></div>
        <div class="empty1"></div>
        <div class="econom"></div>
    </div>
</div>
<!--секция скриптов-->
@section Scripts {
    @Scripts.Render("~/scripts/PlaneSeatsEconom.js")
    @Scripts.Render("~/scripts/PlaneSeatsBuisness.js")
}